# OpenAgent Desktop (skeleton)

Electron + React + TypeScript desktop shell. Lands in MT-010 (Milestone 3).

Planned surface: workspace explorer, editor integration, agent control panel, staged diff review UI.

Until then, all functionality is available via `openagent-cli` (see repository root README).
